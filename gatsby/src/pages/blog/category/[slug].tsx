import { graphql } from "gatsby";
import * as React from "react";

export default function BlogCategoryPage(props: any) {
  console.log(props);

  return (
    <div>
      <p>This is the blog category single page</p>
    </div>
  );
}
